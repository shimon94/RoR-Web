
<table>
<h1>המוצרים שלי</h1>  
<h1>המוצרים שלי</h1>  
<%= link_to 'חזור', home_dashboard_path %>



  <tbody>
    <% @products.each do |product| %>
      <tr>
      <%if product.user == current_user %>
      <div class="d-flex justify-content-center container mt-5">
      <div class="card p-3 bg-white"><i class="fa fa-apple"></i>
          <div class="about-product text-center mt-2"><img src="https://i.imgur.com/3VTaSeb.png" width="300">
              <div>
                  <h4><%= product.title %></h4>
                  <h6 class="mt-0 text-black-50">        
                  <%= product.category %></td>
  </h6>
  
                  <h4><%= product.description %></h4>
  
  
                  <h4>מחיר</h4>
          <div><span><%= product.price %></span></div>
          <%= link_to 'צפה במוצר', product %>
          <%= link_to 'שנה את המוצר', edit_product_path(product) %>
          <%= link_to 'מחק את המוצר', product, method: :delete, data: { confirm: '?בטוח' } %>

          <div>
          
          </div>
          </div>

          <% else %>
  </div>
      
      
      
      </div>
      
  </div>
     


        </tr>
  </tbody>
</table>

<br>


</div>



<div class="d-flex justify-content-center container mt-5">
    <div class="card p-3 bg-white"><i class="fa fa-apple"></i>
        <div class="about-product text-center mt-2"><img src="https://i.imgur.com/3VTaSeb.png" width="300">
            <div>
                <h4><%= product.title %></h4>
                <h6 class="mt-0 text-black-50">        <td><%= product.category %></td>
</h6>

                <h4><%= product.description %></h4>


            </div>
        </div>
        <div class="d-flex justify-content-between total font-weight-bold mt-4"><span> מחיר</span></div>
        <div><span><%= product.price %></span></div>
        <div>
        
        <td><%= link_to 'צפה במוצר', product %></td>
</div>
    
    
    
    </div>
    
</div>
<% end %>

        <% end %>
